export const buttonSnippet = `import Button from "@/components/ui/Button";

export default function Demo() {
  return (
    <Button variant="primary">
      Get Started
    </Button>
  );
}`;

export const cardSnippet = `import {
  Card,
  CardContent,
  CardTitle,
} from "@/components/ui/Card";

<Card>
  <CardContent>
    <CardTitle>Pro Plan</CardTitle>
  </CardContent>
</Card>`;